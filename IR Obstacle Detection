//VCC → 5V
//GND → GND
//OUT → Pin 8

int irPin = 8;

void setup() {
  Serial.begin(9600);
  pinMode(irPin, INPUT);
}

void loop() {

  if(digitalRead(irPin) == LOW){
    Serial.println("Obstacle Detected");
  } else {
    Serial.println("No Obstacle");
  }

  delay(300);
}
